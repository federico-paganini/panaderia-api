document.addEventListener("DOMContentLoaded", function () {
    if(/*usuario normal */)) {
        window.location.href = "basicmenu.html";
    } else {
        /* localStorage.getItem("userType") */
        /* Quitar opciones según tipo de usuario */
    }
});